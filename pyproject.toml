[project]
name = "hackernews-analyzer"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "requests",
    "openai", 
    "python-dotenv",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]